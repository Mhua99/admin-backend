<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mhua.adminbackend.mapper.SysMenuMapper">
    <insert id="insert" keyProperty="id" useGeneratedKeys="true" keyColumn="id">
        INSERT INTO sys_menu (name, parent_id, create_time, update_time, create_user, update_user, url, type, icon)
        VALUES (#{name}, #{parentId}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser}, #{url}, #{type}, #{icon})
    </insert>

    <update id="update">
        update sys_menu
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="url != null and url != ''">
                url = #{url},
            </if>
            <if test="icon != null and icon != ''">
                icon = #{icon},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="parentId != null">
                parent_id = #{parentId},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
        delete from sys_menu where id in
                             <foreach item="id" collection="ids" separator="," open="(" close=")">
                                 #{id}
                             </foreach>
    </delete>

    <select id="list" resultType="com.mhua.adminbackend.pojo.entity.SysMenu">
        SELECT
        m.*,
        p.name AS parentName
        FROM sys_menu m
        LEFT JOIN sys_menu p ON m.parent_id = p.id
        <where>
            <if test="name != null and name != ''">
                m.name like concat('%', #{name}, '%')
            </if>
            <if test="parentId != null">
                m.parent_id = #{parentId}
            </if>
        </where>
    </select>

    <select id="listAll" resultType="com.mhua.adminbackend.pojo.entity.SysMenu">
        SELECT
            m.*,
            p.name AS parentName
        FROM sys_menu m
                 LEFT JOIN sys_menu p ON m.parent_id = p.id
    </select>
</mapper>